/*
 * package model.board;
 * 
 * public class searchDAO { String CpriceSql = ""; String sql_selectAll =
 * "SELECT * FROM (SELECT SearchDTO searchDTO = new SearchDTO();
 * 
 * if (searchDTO.get) StringBuilder searchSb = new StringBuilder();
 * ArrayList<String> searchdata =
 * 
 * }
 */